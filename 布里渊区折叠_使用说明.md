# 布里渊区折叠脚本说明文档

## 先说明：上一版脚本是怎么实现的

上一版 `brillouin_zone_folding_common.py` 做的是“拓扑分析/热图占位入口”，不是严格意义上的 FDTD 超胞构造脚本。它的实现是：

- 读取 `START_NM / END_NM / STEP_NM`，生成一组 `delta L`。
- 用简化 proxy 生成 `gap proxy`、`Q proxy`、`predicted_topological_charge`。
- 画 `folded_zone_heatmap.png`、`topological_charge_and_gap.png` 等图。
- 写入 `manifest.csv`，方便网页和总控看到“有结果”。

它没有打开 `.fsp`，没有复制源 FSP，也没有把一个 cell 改成两个 cell。因此它不能满足论文图 1、图 2 中真正的 Brillouin zone folding 几何过程。

## 现在这版脚本是怎么实现的

现在已改成真实的 FDTD 超胞构造流程。每个母结构都有入口：

`coding\布里渊区折叠\run_fdtd_brillouin_zone_folding_sweep.py`

共享核心在：

`brillouin_zone_folding_common.py`

## 论文思想和代码实现的对应关系

论文里的 Brillouin zone folding 关键动作不是先画图，而是先把仿真单元从一个 cell 变成 x 方向两个 cell 的 supercell：

- 原始结构：`ax = ay = 400 nm` 这一类一周期单胞。
- 折叠结构：沿 x 方向周期翻倍，即 `ax -> 2ax`，所以动量空间里的 Brillouin zone 沿 x 方向减半。
- 物理后果：原来在 zone boundary 附近的 guided mode 被折叠到新的 Γ 附近，形成 BZF-BIC / q-BZF-BIC 相关模态。
- gap perturbation：引入 `ΔL` 后，同一个 supercell 内两个 resonator 的间距从 `L` 改为 `L - ΔL`，改变 intra-cell 与 inter-cell coupling strength。
- 拓扑相变判断：论文里 `ΔL ≈ 39.98 nm` 是 EP / Fermi arc 相关的临界区域；`ΔL < 39.98 nm` 和 `ΔL > 39.98 nm` 应该分别观察 band、Q factor、远场偏振绕数和 BZF-BIC 所在能带是否发生跳迁。

因此代码先做最基础也最容易出错的一步：保护源 `.fsp`，生成 `2-cell supercell + ΔL` 的一系列干净输入文件。后续 band sweep、角分辨谱和 winding number 才应该基于这些超胞文件继续做。

核心流程如下：

1. 保护源文件

脚本只从母结构的 `fsp` 文件夹读取源 `.fsp`，并计算 SHA256。后续每一个扫描点运行前都会重新检查源文件哈希，如果源文件被改动，脚本会停止，避免误改母文件。

2. 复制母文件

每次运行都会创建：

`results\布里渊区折叠\run_test_*\05_work_fsp\master_template.fsp`

这个 `master_template.fsp` 是从源 `.fsp` 复制出来的母文件。每个 `delta L` 扫描点都会再从这个母文件复制一个独立工作副本。

3. 读取周期

脚本优先读取衬底对象的：

- `x span` 作为原始 `Lx = ax`
- `y span` 作为原始 `Ly = ay`

默认候选衬底对象：

`SiO2_substrate`、`substrate`、`Si_substrate`、`Au_substrate`

如果衬底不存在，则退回读取 `FDTD` 区域的 `x span/y span`。这对应你的判断：如果 FDTD 区域包裹着衬底，衬底大小通常就是周期大小。

4. 构造布里渊区折叠超胞

论文思想是：

`ax -> 2ax`

也就是 x 方向周期加倍，Brillouin zone 在 x 方向减半。代码中会把这些对象的 `x span` 改成 `2Lx`：

- `FDTD`
- `SiO2_substrate`
- `Au_substrate`
- `substrate`
- `source`
- `T`

`y span` 保持 `Ly`。

5. 复制 resonator motif

每个母结构入口脚本里都有：

```python
MOTIF_OBJECT_NAMES = (...)
```

例如双圆盘是：

```python
MOTIF_OBJECT_NAMES = ('Si_disk',)
```

脚本会把原来的 motif 先整体移动到左侧：

`x = x - 0.5 * (Lx - deltaL)`

然后复制一份到右侧：

`copy(Lx - deltaL, 0, 0)`

因此两个 cell 的中心距离就是：

`L - deltaL`

这对应你图里从 `L` 到 `L - ΔL` 的 gap perturbation。

6. 输出每个扫描点的 FSP

每个扫描点保存为：

`01_supercell_fsp\0000_BZF_deltaL_000.000nm.fsp`

命名能直接看到当前扰动步长/扰动值。

## 运行方式

只生成计划，不打开 FDTD：

```powershell
python run_fdtd_brillouin_zone_folding_sweep.py --mode preview
```

生成前 3 个超胞 FSP：

```powershell
python run_fdtd_brillouin_zone_folding_sweep.py --mode test
```

生成全部超胞 FSP：

```powershell
python run_fdtd_brillouin_zone_folding_sweep.py --mode full
```

显示 FDTD GUI 调试：

```powershell
python run_fdtd_brillouin_zone_folding_sweep.py --mode test --show-gui
```

## 顶部可修改参数

网页总控和 `fdtd_master_controller.py` 会识别这些顶层变量：

```python
START_NM = 0.0
END_NM = 60.0
STEP_NM = 5.0
SIMULATION_TIME_S = 5e-11
AUTO_SHUTOFF_MIN = 1e-7
```

如果要围绕论文里的临界点 `ΔL = 39.98 nm` 细扫，可以改成：

```python
START_NM = 35.0
END_NM = 45.0
STEP_NM = 1.0
```

## 输出目录

每次 `test/full` 会生成：

- `00_scan_plan\scan_points.csv`：扫描计划。
- `01_supercell_fsp\*.fsp`：真正修改后的双 cell 超胞 FSP。
- `02_topology_metrics\bzf_supercell_metrics.csv`：记录 `Lx`、`2Lx`、`L-deltaL` 等几何参数。
- `03_brillouin_folding_png\bzf_deltaL_center_distance.png`：`delta L` 与 cell 间距关系图。
- `04_logs\manifest.csv`：网页/总控可读取的运行状态。
- `05_work_fsp\master_template.fsp`：从源文件复制出来的母文件。
- `布里渊区折叠_脚本实现说明.md`：本次运行生成的详细说明。

## 重要注意

当前脚本的 `test/full` 已不再只是搭建结构：它会先生成 `2-cell supercell + ΔL` 的 `.fsp`，然后运行 FDTD，读取 `T` monitor，输出 Excel 和透射谱 PNG。`preview` 仍然只生成扫描计划，不打开 FDTD。

它仍然不是完整的拓扑荷计算。它把论文中 BZF 的几何第一步和普通透射谱仿真做出来：`1 cell -> 2 cell supercell`，并引入 `ΔL`。下一步如果要证明拓扑相变，需要继续做：

1. 对这些超胞 FSP 做角分辨透射/反射谱或 band sweep。
2. 提取 Γ 附近上下两条 band 的 Q 因子。
3. 提取远场偏振 `S1/S2` 或相位图。
4. 沿闭合路径计算 winding number/topological charge。
5. 对比 `ΔL < 39.98 nm`、`ΔL ≈ 39.98 nm`、`ΔL > 39.98 nm` 的 band inversion 和 BZF-BIC 跳迁。
