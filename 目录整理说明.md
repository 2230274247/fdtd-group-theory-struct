# 群论_struct 目录整理说明

这个文件只做一件事：告诉 AI 和人类，哪些东西放在哪类文件夹里。

## 当前分类

```text
00_总览与说明/      项目背景、用户指南、目录总结
01_脚本说明/        各类脚本的使用说明
90_备份与缓存/      bak、提取中间文本、临时导出
docs/               全局报告、验收、网页可视化、BZF 相关材料
经验文档库/         给 AI 用的结构化经验库，优先读 JSON
```

## 使用原则

1. 先看 `00_总览与说明`，理解项目是什么。
2. 再看 `经验文档库`，让 AI 快速决定用什么机制和什么结构族。
3. 再进入具体结构族目录看脚本与结果。
4. 备份、缓存、中间文本不要作为主线结论依据。
5. 根层的 `fdtd_master_controller.py`、`fdtd_results_manager.py`、`fdtd_autotune_common.py`、`brillouin_zone_folding_common.py` 先保留在原处，因为它们被大量脚本和历史运行记录直接引用；移动它们会立刻破坏启动路径和 import 关系。
